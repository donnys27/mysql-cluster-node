type: update
jpsVersion: '1.0.0'
name: MariaDB Node

baseUrl: https://raw.githubusercontent.com/donnys27/mysql-cluster-node/master

settings:
  fields:
   - type: list
     caption: MariaDB Node
     values:
      value1: "${nodes.sqldb[0].id}"
      value2: "${nodes.sqldb[1].id}"

#onInstall:

actions:
  deleteTable:
    - cmd [${nodes.sqldb[0].id}]:
       - mysql -uroot -pwQrSbd4yGecJbStM --execute="DROP DATABASE repltest"